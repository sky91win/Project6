pipeline {
    agent any

    stages {
        stage('DEBUG') {
            steps {
                echo "🔥 Jenkinsfile is executing"
                sh 'pwd'
                sh 'ls -la'
            }
        }
    }
}
